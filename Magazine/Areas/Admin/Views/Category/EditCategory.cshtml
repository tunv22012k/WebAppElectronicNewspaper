@model Magazine.Models.Category

@{ 
    ViewBag.Title = "Chỉnh sửa chuyên mục cha";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml"; 
}

<h2>Chỉnh sửa chuyên mục cha</h2>

@using (Html.BeginForm("EditCategory", "Category", FormMethod.Post, new { area = "Admin" }))
{
    @Html.AntiForgeryToken()
    @Html.HiddenFor(model => model.CategoryID)

    <div class="form-horizontal">
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })

        <div class="form-group">
            <div>
                @Html.EditorFor(model => model.CategoryName, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.CategoryName, "", new { @class = "text-danger" })
            </div>
        </div>
        <p></p>
        <div class="form-group">
            <button type="submit" class="btn btn-success">Lưu thay đổi</button>
            @Html.ActionLink("Quay lại", "Index", "Category", new { area = "Admin" }, new { @class = "btn btn-outline-secondary" })
        </div>
    </div>
}